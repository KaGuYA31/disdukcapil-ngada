{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && npx prisma db push --accept-data-loss 2>/dev/null; next build",
  "git": {
    "deploymentEnabled": true
  }
}
